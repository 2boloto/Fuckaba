// Начинает получение байтов из сети.

// >- 0

// <- r - значение, которое должно сохраняться между вызовами всех получающих модулей

// После вызова этого модуля и вплоть до вызова `end-reveiving.txt` любой ввод-вывод, кроме документированного для этих модулей, - нельзя. Если какой-то модуль вернёт ошибку соединения, то дальше получать байты из этого подключения нельзя.

#INC 1
